{
  "entities": {
    "Employee": {
      "title": "Employee",
      "description": "Represents a crew member in the payroll system.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the employee."
        },
        "hourlyRate": {
          "type": "number",
          "description": "The base hourly rate for the employee."
        },
        "onTimeBonus": {
          "type": "number",
          "description": "The hourly bonus rate if the employee clocks in on time."
        },
        "rules": {
          "type": "string",
          "description": "Custom payroll rules for the employee."
        }
      },
      "required": [
        "name",
        "hourlyRate",
        "onTimeBonus",
        "rules"
      ]
    },
    "TimeEntry": {
      "title": "TimeEntry",
      "description": "Represents a single clock-in/clock-out event for an employee.",
      "type": "object",
      "properties": {
        "employeeId": {
          "type": "string",
          "description": "The ID of the employee this time entry belongs to."
        },
        "clockIn": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the employee clocked in."
        },
        "clockOut": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the employee clocked out."
        },
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The date of the time entry."
        }
      },
      "required": [
        "employeeId",
        "date"
      ]
    },
    "Bonus": {
      "title": "Bonus",
      "description": "Represents a one-time bonus given to an employee for a specific week.",
      "type": "object",
      "properties": {
        "employeeId": {
          "type": "string",
          "description": "The ID of the employee this bonus belongs to."
        },
        "amount": {
          "type": "number",
          "description": "The monetary amount of the bonus."
        },
        "reason": {
          "type": "string",
          "description": "The reason for giving the bonus."
        },
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The start date of the week for which the bonus is awarded."
        }
      },
      "required": [
        "employeeId",
        "amount",
        "reason",
        "date"
      ]
    }
  },
  "auth": {
    "providers": [
      "email-password"
    ]
  },
  "firestore": {
    "/employees/{employeeId}": {
      "schema": {
        "$ref": "#/entities/Employee"
      },
      "description": "Stores information about individual employees."
    },
    "/timeEntries/{timeEntryId}": {
      "schema": {
        "$ref": "#/entities/TimeEntry"
      },
      "description": "Stores all time clock-in and clock-out entries."
    },
    "/bonuses/{bonusId}": {
      "schema": {
        "$ref": "#/entities/Bonus"
      },
      "description": "Stores weekly performance bonuses for employees."
    }
  }
}
